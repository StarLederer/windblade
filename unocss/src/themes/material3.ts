import type { Theme } from "../theme";

const theme: Theme = {
  windblade: {
    colors: {
      // TODO: Fill this
    },

    proportions: {
      'extrasmall': 0.25,
      'small': 0.5,
      'medium': 0.75,
      'large': 1,
      'extralarge': 1.75,
    },

    time: {
      baseUnitMs: 150,

      functions: {
        default: 'cubic-bezier(0.4, 0, 0.2, 1)',

        // TODO: Fill this
      },
    }
  },
};

export default theme;
