import { layout } from "../../unocss/rules/documented";

const data = [
  layout.aspectRatio().docs,
  layout.columns().docs,
  layout.breakAfter().docs,
  layout.breakBefore().docs,
  layout.breakInside().docs,
  layout.boxDecorationBreak().docs,
  layout.boxSizing().docs,
  layout.display().docs,
  layout.floats().docs,
  layout.clear().docs,
];

export default data;
